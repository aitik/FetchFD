import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  root: {
    marginTop: 10,
  },
  select: {
    minWidth: 200,
  },
  galleryContainer: {
    marginTop: 10,
    // display: 
  },
  image: {
    maxWidth: '100%',
  },
}));

export default useStyles;
